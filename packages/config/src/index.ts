/**
 * Shared app constants (brand, URLs). Expand in later phases.
 */
export const APP_NAME = 'RealWorkStudio';

export const APP_TAGLINE = 'Learn by shipping real work — not toy tutorials.';

/** Default max content width (matches PRD ~1280px) */
export const LAYOUT_MAX_WIDTH_PX = 1280;
